import type { Metadata } from "next";
import Link from "next/link";
import { resolveCrmSession } from "@/lib/firebase/session";
import { LogoutButton } from "@/components/crm/LogoutButton";
export const metadata:Metadata={title:"Administration communautaire",robots:{index:false,follow:false}};
const domains=["Contributions","Relecture","Organisations","Fichiers privés","Journal d’audit"];
const editorial=["draft.self.manage","review.assigned.read","review.assigned.comment","editorial.assign","editorial.completeness.review","editorial.provenance.verify","editorial.rights.verify","editorial.consent.verify","editorial.ordinary.approve"];
export default async function CrmPage(){const context=await resolveCrmSession();const permissions=context.state==="authorized"?context.permissions:[];const hasEditorial=permissions.some(permission=>editorial.includes(permission));return <main className="crm-shell"><header className="crm-shell-header"><div><p className="crm-kicker">Espace privé</p><h1>Administration communautaire</h1><p>Session de <strong>{context.state==="authorized"?context.displayName:"membre autorisé"}</strong>.</p></div><LogoutButton/></header>{context.state==="authorized"&&<section aria-labelledby="active-domain"><h2 id="active-domain">Outils disponibles</h2><div className="crm-tool-grid">{hasEditorial&&<Link className="crm-tool-card" href="/crm/contributions"><strong>Contributions éditoriales</strong><span>Ouvrir le circuit privé →</span></Link>}{permissions.includes("profile.assigned.read")&&<Link className="crm-tool-card" href="/crm/comptes"><strong>Comptes et rôles globaux</strong><span>Ouvrir la gestion privée →</span></Link>}{permissions.some(permission=>["organization.list","organization.member.manage","role.local.manage"].includes(permission))&&<Link className="crm-tool-card" href="/crm/organisations"><strong>Organisations et appartenances</strong><span>Ouvrir la gestion cloisonnée →</span></Link>}</div></section>}<section aria-labelledby="future-domains"><h2 id="future-domains">Domaines à venir</h2><ul className="crm-domain-list">{domains.filter(domain=>domain!=="Organisations"&&(!hasEditorial||!["Contributions","Relecture"].includes(domain))).map((domain,index)=><li key={domain}><span>{String(index+1).padStart(2,"0")}</span><strong>{domain}</strong><small>Indisponible</small></li>)}</ul></section></main>}
