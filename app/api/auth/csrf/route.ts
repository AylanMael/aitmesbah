import { NextRequest,NextResponse } from "next/server";
import { generateCsrfToken } from "@/lib/crm/session-policy.mjs";
import { PRIVATE_RESPONSE_HEADERS,validateRequestSecurity } from "@/lib/crm/request-security.mjs";
import { createServerConfig } from "@/lib/config/server-config.mjs";
export async function GET(request:NextRequest){try{const config=createServerConfig(process.env);validateRequestSecurity({kind:"read",method:request.method,host:request.headers.get("host"),forwardedHost:request.headers.get("x-forwarded-host"),origin:request.headers.get("origin"),fetchSite:request.headers.get("sec-fetch-site"),fetchMode:request.headers.get("sec-fetch-mode"),fetchDestination:request.headers.get("sec-fetch-dest"),allowedHosts:config.hosts,allowedOrigins:config.origins});const token=generateCsrfToken(),response=NextResponse.json({csrfToken:token},{headers:PRIVATE_RESPONSE_HEADERS});response.cookies.set({...config.csrfCookie,value:token});return response;}catch{return NextResponse.json({error:"Requête interdite."},{status:403,headers:PRIVATE_RESPONSE_HEADERS});}}
