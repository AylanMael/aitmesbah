import test from "node:test";import assert from "node:assert/strict";import {preparePublicContentPublication,preparePublicContentUnpublication} from "../../lib/crm/public-content-publication.mjs";
const now={seconds:10},base={contributionId:"contribution-abcdef123",title:"Une archive du village",summary:"Une notice documentaire complète",category:"photographs_archives",status:"approved",sensitivity:"ordinary",organizationId:null,updatedAt:now,updatedBy:"editor",version:4,currentVersion:1},version={number:1,contributionId:"contribution-abcdef123",body:"Contenu éditorial validé et destiné à la publication."},context={actorUid:"editor",actorStatus:"active",permissions:["editorial.ordinary.publish"],primaryAssetId:"asset-123",primaryAssetMimeType:"image/jpeg",now};
test("publie une archive approuvée avec une URL stable et un média validé",()=>{const result=preparePublicContentPublication(base,version,{kind:"archive",reason:"publication validée"},context);assert.equal(result.publication.kind,"archive");assert.equal(result.publication.primaryAssetId,"asset-123");assert.equal(result.publication.primaryAssetMimeType,"image/jpeg");assert.match(result.publication.slug,/une-archive-du-village-/);assert.equal(result.contribution.status,"published");});
test("refuse les formats incompatibles et les dossiers non approuvés",()=>{assert.throws(()=>preparePublicContentPublication(base,version,{kind:"news",reason:"non"},context));assert.throws(()=>preparePublicContentPublication({...base,status:"submitted"},version,{kind:"archive",reason:"non"},context));});
test("dépublie sans supprimer la projection ni changer son URL",()=>{const first=preparePublicContentPublication(base,version,{kind:"photo",reason:"publication validée"},context);const result=preparePublicContentUnpublication(first.contribution,first.publication,"retrait éditorial",context);assert.equal(result.publication.status,"unpublished");assert.equal(result.publication.slug,first.publication.slug);});
