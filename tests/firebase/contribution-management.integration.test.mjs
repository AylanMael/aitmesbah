import test from "node:test";import assert from "node:assert/strict";import {getLocalAdminServices} from "../../scripts/firebase/local-account-admin.mjs";import {assertLocalEmulatorSafety} from "./test-helpers.mjs";
test("versions et décisions restent privées et immuables dans le projet démo",async()=>{assertLocalEmulatorSafety();const {database}=getLocalAdminServices(),id="contribution-8l-fixture",root=database.doc(`contributions/${id}`),version=root.collection("versions").doc("v1");try{await root.create({contributionId:id,status:"draft",authorUid:"author-8l"});await version.create({versionId:"v1",number:1,body:"Texte fictif",integrity:"a".repeat(64)});await assert.rejects(version.create({versionId:"v1",number:1,body:"Écrasement"}));assert.equal((await version.get()).data().body,"Texte fictif");}finally{await database.recursiveDelete(root);}});
