"use client";
import { FormEvent,useRef,useState } from "react";
import { signInWithEmailAndPassword,signOut } from "firebase/auth";
import { getLocalFirebaseAuth } from "@/lib/firebase/client";
export function LoginForm(){const formRef=useRef<HTMLFormElement>(null);const [busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(busy)return;setBusy(true);setError("");const data=new FormData(event.currentTarget);try{const csrfResponse=await fetch("/api/auth/csrf",{cache:"no-store"});if(!csrfResponse.ok)throw new Error();const {csrfToken}=await csrfResponse.json();const auth=await getLocalFirebaseAuth();const credential=await signInWithEmailAndPassword(auth,String(data.get("email")),String(data.get("password")));const idToken=await credential.user.getIdToken();const response=await fetch("/api/auth/session",{method:"POST",headers:{"Content-Type":"application/json","X-CSRF-Token":csrfToken},body:JSON.stringify({idToken})});await signOut(auth);if(!response.ok)throw new Error();window.location.assign("/crm");}catch{setError("Connexion impossible. Vérifiez vos informations ou contactez un responsable.");setBusy(false);}}return <form ref={formRef} className="crm-login-form" onSubmit={submit} aria-busy={busy}><div><label htmlFor="crm-email">Adresse électronique</label><input id="crm-email" name="email" type="email" autoComplete="username" required disabled={busy}/></div><div><label htmlFor="crm-password">Mot de passe</label><input id="crm-password" name="password" type="password" autoComplete="current-password" required disabled={busy}/></div>{error&&<p className="crm-form-error" role="alert">{error}</p>}<button type="button" onClick={()=>formRef.current?.requestSubmit()} disabled={busy}>{busy?"Connexion en cours…":"Se connecter"}</button></form>}
